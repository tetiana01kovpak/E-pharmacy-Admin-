# Local dev: leave unset, relative /api is proxied to the backend via vite.config.js
# Production (e.g. Vercel): point this at your deployed backend, e.g. https://your-backend.onrender.com/api
VITE_API_BASE_URL=
